package view

import "gosuda.org/website/internal/types"

templ GosudaBlogPost(doc *types.Document, post *types.Post) {
	<div class="max-w-6xl mx-auto p-4 min-h-screen flex flex-col">
		@BlogHeader()
		<div class="flex flex-col lg:flex-row flex-grow"></div>
		@BlogFooter()
	</div>
}
